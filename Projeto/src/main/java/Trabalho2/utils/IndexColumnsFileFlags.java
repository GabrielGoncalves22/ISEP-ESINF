package Trabalho2.utils;

/**
 * 'Interface' que contêm a posição de cada coluna do ficheiro das bandeiras
 */
public interface IndexColumnsFileFlags {
    int INDEX_FLAG = 0;
    int INDEX_FLAG_DESCRIPTION = 1;

    int TOTAL_COLUMNS_FILE_FLAGS = 2;
}
